#include "main.h"

/**
 * pass - function that prints characters with _putcharr
 * Return: is Always 0
 */

int pass(void)
{
	int x = 0x48;
	int y = 0x6F;
	int z = 0x6C;
	int p = 0x04;

	_putcharr(x);
	return (0);
}
